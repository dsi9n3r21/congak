/** Support/feedback constants, used by SupportFooter wherever it
 * appears (public homepage, parent dashboard, student profile).
 *
 * This used to back a full standalone /coffee explainer page — removed
 * per Lynda's request to keep this simple: "Buy Congak a Coffee" now
 * links straight to Ko-fi instead of an in-between page. Trimmed down
 * to just what's still actually used; the removed page/copy explained
 * what tips fund (hosting + Pintar's AI tokens) and made clear support
 * is optional — worth keeping in mind if this ever needs restoring or
 * that context needs to live somewhere else (e.g. directly on the Ko-fi
 * page's own description, which Lynda controls there).
 */
export const COFFEE = {
  koFiUrl: "https://ko-fi.com/razsoul",
  feedbackEmail: "razsoulconsultancy@gmail.com",
};
